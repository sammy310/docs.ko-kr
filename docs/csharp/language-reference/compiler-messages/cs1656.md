---
description: 컴파일러 오류 CS1656
title: 컴파일러 오류 CS1656
ms.date: 07/20/2015
f1_keywords:
- CS1656
helpviewer_keywords:
- CS1656
ms.assetid: b5463a12-d685-4dae-9f88-08383e271b7a
ms.openlocfilehash: ea673ffb33f1e4e2f25bd2cd3f518969cbc60418
ms.sourcegitcommit: 5b475c1855b32cf78d2d1bbb4295e4c236f39464
ms.translationtype: HT
ms.contentlocale: ko-KR
ms.lasthandoff: 09/24/2020
ms.locfileid: "91175163"
---
# <a name="compiler-error-cs1656"></a><span data-ttu-id="2e477-103">컴파일러 오류 CS1656</span><span class="sxs-lookup"><span data-stu-id="2e477-103">Compiler Error CS1656</span></span>

<span data-ttu-id="2e477-104">‘read-only variable type’이기 때문에 ‘variable’에 할당할 수 없습니다.</span><span class="sxs-lookup"><span data-stu-id="2e477-104">Cannot assign to 'variable' because it is a 'read-only variable type'</span></span>  
  
 <span data-ttu-id="2e477-105">이 오류는 읽기 전용 컨텍스트에서 변수에 할당할 때 발생합니다.</span><span class="sxs-lookup"><span data-stu-id="2e477-105">This error occurs when an assignment to variable occurs in a read-only context.</span></span> <span data-ttu-id="2e477-106">읽기 전용 컨텍스트에는 [foreach](../keywords/foreach-in.md) 반복 변수, [using](../keywords/using-statement.md) 변수 및 [fixed](../keywords/fixed-statement.md) 변수가 포함됩니다.</span><span class="sxs-lookup"><span data-stu-id="2e477-106">Read-only contexts include [foreach](../keywords/foreach-in.md) iteration variables, [using](../keywords/using-statement.md) variables, and [fixed](../keywords/fixed-statement.md) variables.</span></span> <span data-ttu-id="2e477-107">이 오류를 해결하려면 `using` 블록, `foreach` 문 및 `fixed` 문에서 문 변수에 할당하지 않아야 합니다.</span><span class="sxs-lookup"><span data-stu-id="2e477-107">To resolve this error, avoid assignments to a statement variable in `using` blocks, `foreach` statements, and `fixed` statements.</span></span>  
  
## <a name="example"></a><span data-ttu-id="2e477-108">예제</span><span class="sxs-lookup"><span data-stu-id="2e477-108">Example</span></span>  

 <span data-ttu-id="2e477-109">다음 예제에서는 `foreach` 루프 내에 있는 컬렉션의 전체 요소를 교체하려고 하기 때문에 CS1656 오류가 발생합니다.</span><span class="sxs-lookup"><span data-stu-id="2e477-109">The following example generates error CS1656 because it tries to replace complete elements of a collection inside a `foreach` loop.</span></span> <span data-ttu-id="2e477-110">오류를 해결하는 한 가지 방법은 `foreach` 루프를 [for](../keywords/for.md) 루프로 변경하는 것입니다.</span><span class="sxs-lookup"><span data-stu-id="2e477-110">One way to work around the error is to change the `foreach` loop to a [for](../keywords/for.md) loop.</span></span> <span data-ttu-id="2e477-111">여기에 표시되지 않은 또 한 가지 방법은 기존 요소의 멤버를 수정하는 것인데, 이는 클래스에서는 가능하지만 구조체에서는 가능하지 않습니다.</span><span class="sxs-lookup"><span data-stu-id="2e477-111">Another way, not shown here, is to modify the members of the existing element; this is possible with classes, but not with structs.</span></span>  
  
```csharp  
using System;  
using System.Collections;  
using System.Collections.Generic;  
using System.Text;  
  
namespace CS1656_2  
{  
  
    class Book  
    {  
        public string Title;  
        public string Author;  
        public double Price;  
        public Book(string t, string a, double p)  
        {  
            Title=t;  
            Author=a;  
            Price=p;  
  
        }  
    }  
  
    class Program  
    {  
        private List<Book> list;  
        static void Main(string[] args)  
        {  
            Program prog = new Program();  
            prog.list = new List<Book>();  
            prog.list.Add(new Book ("The C# Programming Language",  
                                    "Hejlsberg, Wiltamuth, Golde",  
                                     29.95));  
            prog.list.Add(new Book ("The C++ Programming Language",  
                                    "Stroustrup",  
                                     29.95));  
            prog.list.Add(new Book ("The C Programming Language",  
                                    "Kernighan, Ritchie",  
                                    29.95));  
            foreach(Book b in prog.list)  
            {  
                // Cannot modify an entire element in a foreach loop
                // even with reference types.  
                // Use a for or while loop instead  
                if (b.Title == "The C Programming Language")  
                    // Cannot assign to 'b' because it is a 'foreach
                    // iteration variable'  
                    b = new Book("Programming Windows, 5th Ed.", "Petzold", 29.95); //CS1656  
            }  
  
            //With a for loop you can modify elements  
            //for(int x = 0; x < prog.list.Count; x++)  
            //{  
            //    if(prog.list[x].Title== "The C Programming Language")  
            //        prog.list[x] = new Book("Programming Windows, 5th Ed.", "Petzold", 29.95);  
            //}  
            //foreach(Book b in prog.list)  
            //    Console.WriteLine(b.Title);  
  
        }  
    }  
}  
```  
  
## <a name="example"></a><span data-ttu-id="2e477-112">예제</span><span class="sxs-lookup"><span data-stu-id="2e477-112">Example</span></span>  

 <span data-ttu-id="2e477-113">다음 샘플은 `foreach` 루프 이외의 다른 컨텍스트에서 CS1656 오류가 어떻게 발생하는지 보여 줍니다.</span><span class="sxs-lookup"><span data-stu-id="2e477-113">The following sample demonstrates how CS1656 can be generated in other contexts besides a `foreach` loop:</span></span>  
  
```csharp  
// CS1656.cs  
// compile with: /unsafe  
using System;  
  
class C : IDisposable  
{  
    public void Dispose() { }  
}  
  
class CMain  
{  
    unsafe public static void Main()  
    {  
        using (C c = new C())  
        {  
            // Cannot assign to 'c' because it is a 'using variable'  
            c = new C(); // CS1656  
        }  
  
        int[] ary = new int[] { 1, 2, 3, 4 };  
        fixed (int* p = ary)  
        {  
            // Cannot assign to 'p' because it is a 'fixed variable'  
            p = null; // CS1656  
        }  
    }  
}  
```

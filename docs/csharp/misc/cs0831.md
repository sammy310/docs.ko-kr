---
title: 컴파일러 오류 CS0831
ms.date: 07/20/2015
f1_keywords:
- CS0831
helpviewer_keywords:
- CS0831
ms.assetid: f626a77d-3844-4438-954b-b8201e72b1b5
ms.openlocfilehash: eb341bfc3f0f502561f2321e5be8c6db2415b465
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: ko-KR
ms.lasthandoff: 03/12/2020
ms.locfileid: "79171718"
---
# <a name="compiler-error-cs0831"></a><span data-ttu-id="7dd4d-102">컴파일러 오류 CS0831</span><span class="sxs-lookup"><span data-stu-id="7dd4d-102">Compiler Error CS0831</span></span>
<span data-ttu-id="7dd4d-103">식 트리에는 기본 액세스를 사용할 수 없습니다.</span><span class="sxs-lookup"><span data-stu-id="7dd4d-103">An expression tree may not contain a base access.</span></span>  
  
 <span data-ttu-id="7dd4d-104">기본 클래스 메서드의 비가상 함수 호출처럼 일반적으로 가상 함수 호출인 함수를 만드는 것이 기본 액세스입니다.</span><span class="sxs-lookup"><span data-stu-id="7dd4d-104">A base access means to make a function call that would ordinarily be a virtual function call as a non-virtual function call on the base class method.</span></span> <span data-ttu-id="7dd4d-105">식 트리 자체에서는 허용되지 않지만 기본 클래스 메서드를 호출하는 클래스에서는 도우미 메서드를 호출할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="7dd4d-105">This is not allowed in the expression tree itself, but you can invoke a helper method in your class that invokes the base class method.</span></span>  
  
## <a name="to-correct-this-error"></a><span data-ttu-id="7dd4d-106">이 오류를 해결하려면</span><span class="sxs-lookup"><span data-stu-id="7dd4d-106">To correct this error</span></span>  
  
1. <span data-ttu-id="7dd4d-107">이런 방식으로 기본 클래스 메서드에 액세스해야 하는 경우 기본 클래스로 호출하는 도우미 메서드를 만들어서 식 트리가 도우미 메서드를 호출하게 합니다.</span><span class="sxs-lookup"><span data-stu-id="7dd4d-107">If you must access a base class method in this manner, create a helper method that calls into the base class and have the expression tree call the helper method.</span></span> <span data-ttu-id="7dd4d-108">이 기술이 다음 코드에 나옵니다.</span><span class="sxs-lookup"><span data-stu-id="7dd4d-108">This technique is shown in the following code.</span></span>  
  
## <a name="example"></a><span data-ttu-id="7dd4d-109">예제</span><span class="sxs-lookup"><span data-stu-id="7dd4d-109">Example</span></span>  
 <span data-ttu-id="7dd4d-110">다음 예제에서는 CS0831을 생성합니다.</span><span class="sxs-lookup"><span data-stu-id="7dd4d-110">The following example generates CS0831:</span></span>  
  
```csharp  
// cs0831.cs  
using System;  
using System.Linq;  
using System.Linq.Expressions;  
  
public class A  
{  
    public virtual int BaseMethod() { return 1; }  
}  
public class C : A  
{  
    public override int BaseMethod() { return 2; }  
    public int Test(C c)  
    {  
        Expression<Func<int>> e = () => base.BaseMethod(); // CS0831  
  
        // Try the following line instead,
        // along with the BaseAccessHelper method.  
        // Expression<Func<int>> e2 = () => BaseAccessHelper();  
        return 1;  
    }
    // Uncomment to call from e2 expression above.  
    // int BaseAccessHelper()  
    // {  
    //     return base.BaseMethod();  
    // }  
  
}
```

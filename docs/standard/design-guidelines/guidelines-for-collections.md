---
title: 컬렉션에 대한 지침
ms.date: 10/22/2008
ms.technology: dotnet-standard
ms.assetid: 297b8f1d-b11f-4dc6-960a-8e990817304e
ms.openlocfilehash: 50497de6569b448ab036af8a1fbf76a47565e2bb
ms.sourcegitcommit: de17a7a0a37042f0d4406f5ae5393531caeb25ba
ms.translationtype: MT
ms.contentlocale: ko-KR
ms.lasthandoff: 01/24/2020
ms.locfileid: "76741869"
---
# <a name="guidelines-for-collections"></a><span data-ttu-id="905a1-102">컬렉션에 대한 지침</span><span class="sxs-lookup"><span data-stu-id="905a1-102">Guidelines for Collections</span></span>
<span data-ttu-id="905a1-103">몇 가지 공통적인 특징이 있는 개체 그룹을 조작 하도록 특별히 디자인 된 모든 형식은 컬렉션으로 간주할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-103">Any type designed specifically to manipulate a group of objects having some common characteristic can be considered a collection.</span></span> <span data-ttu-id="905a1-104"><xref:System.Collections.IEnumerable> 또는 <xref:System.Collections.Generic.IEnumerable%601>를 구현 하는 이러한 형식에는 거의 항상 적합 합니다. 따라서이 섹션에서는 이러한 인터페이스 중 하나 또는 둘 다를 구현 하는 형식만 컬렉션으로 간주 합니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-104">It is almost always appropriate for such types to implement <xref:System.Collections.IEnumerable> or <xref:System.Collections.Generic.IEnumerable%601>, so in this section we only consider types implementing one or both of those interfaces to be collections.</span></span>

 <span data-ttu-id="905a1-105">공용 Api에서 약하게 형식화 된 컬렉션을 사용 하지 ❌.</span><span class="sxs-lookup"><span data-stu-id="905a1-105">❌ DO NOT use weakly typed collections in public APIs.</span></span>

 <span data-ttu-id="905a1-106">컬렉션 항목을 나타내는 모든 반환 값 및 매개 변수의 형식은 기본 형식이 아닌 정확한 항목 형식 이어야 합니다 .이는 컬렉션의 public 멤버에만 적용 됩니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-106">The type of all return values and parameters representing collection items should be the exact item type, not any of its base types (this applies only to public members of the collection).</span></span>

 <span data-ttu-id="905a1-107">❌ 공용 Api에서 <xref:System.Collections.ArrayList> 또는 <xref:System.Collections.Generic.List%601>를 사용 하지 않습니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-107">❌ DO NOT use <xref:System.Collections.ArrayList> or <xref:System.Collections.Generic.List%601> in public APIs.</span></span>

 <span data-ttu-id="905a1-108">이러한 형식은 공용 Api가 아닌 내부 구현에서 사용 하도록 디자인 된 데이터 구조입니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-108">These types are data structures designed to be used in internal implementation, not in public APIs.</span></span> <span data-ttu-id="905a1-109">`List<T>`는 Api 및 유연성의 정리 비용으로 성능 및 기능에 최적화 되어 있습니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-109">`List<T>` is optimized for performance and power at the cost of cleanness of the APIs and flexibility.</span></span> <span data-ttu-id="905a1-110">예를 들어 `List<T>`반환 하는 경우 클라이언트 코드에서 컬렉션을 수정할 때 알림을 받을 수 없습니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-110">For example, if you return `List<T>`, you will not ever be able to receive notifications when client code modifies the collection.</span></span> <span data-ttu-id="905a1-111">또한 `List<T>`는 <xref:System.Collections.Generic.List%601.BinarySearch%2A>와 같이 많은 시나리오에서 유용 하지 않거나 적용 되지 않는 많은 멤버를 노출 합니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-111">Also, `List<T>` exposes many members, such as <xref:System.Collections.Generic.List%601.BinarySearch%2A>, that are not useful or applicable in many scenarios.</span></span> <span data-ttu-id="905a1-112">다음 두 섹션에서는 공용 Api에서 사용 하기 위해 특별히 설계 된 형식 (추상화)을 설명 합니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-112">The following two sections describe types (abstractions) designed specifically for use in public APIs.</span></span>

 <span data-ttu-id="905a1-113">❌ 공용 Api에서 `Hashtable` 또는 `Dictionary<TKey,TValue>`를 사용 하지 않습니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-113">❌ DO NOT use `Hashtable` or `Dictionary<TKey,TValue>` in public APIs.</span></span>

 <span data-ttu-id="905a1-114">이러한 형식은 내부 구현에서 사용 하도록 디자인 된 데이터 구조입니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-114">These types are data structures designed to be used in internal implementation.</span></span> <span data-ttu-id="905a1-115">공용 Api는 인터페이스 중 하나 또는 둘 다를 구현 하는 <xref:System.Collections.IDictionary>, `IDictionary <TKey, TValue>`또는 사용자 지정 형식을 사용 해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-115">Public APIs should use <xref:System.Collections.IDictionary>, `IDictionary <TKey, TValue>`, or a custom type implementing one or both of the interfaces.</span></span>

 <span data-ttu-id="905a1-116">`GetEnumerator` 메서드의 반환 형식을 제외 하 고 이러한 인터페이스 중 하나를 구현 하는 <xref:System.Collections.Generic.IEnumerator%601>, <xref:System.Collections.IEnumerator>또는 다른 형식을 사용 하지 ❌.</span><span class="sxs-lookup"><span data-stu-id="905a1-116">❌ DO NOT use <xref:System.Collections.Generic.IEnumerator%601>, <xref:System.Collections.IEnumerator>, or any other type that implements either of these interfaces, except as the return type of a `GetEnumerator` method.</span></span>

 <span data-ttu-id="905a1-117">`GetEnumerator` 아닌 메서드에서 열거자를 반환 하는 형식은 `foreach` 문과 함께 사용할 수 없습니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-117">Types returning enumerators from methods other than `GetEnumerator` cannot be used with the `foreach` statement.</span></span>

 <span data-ttu-id="905a1-118">❌는 `IEnumerator<T>`와 `IEnumerable<T>`을 동일한 형식으로 구현 하지 않습니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-118">❌ DO NOT implement both `IEnumerator<T>` and `IEnumerable<T>` on the same type.</span></span> <span data-ttu-id="905a1-119">`IEnumerator` 및 `IEnumerable`제네릭이 아닌 인터페이스에도 동일 하 게 적용 됩니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-119">The same applies to the nongeneric interfaces `IEnumerator` and `IEnumerable`.</span></span>

## <a name="collection-parameters"></a><span data-ttu-id="905a1-120">컬렉션 매개 변수</span><span class="sxs-lookup"><span data-stu-id="905a1-120">Collection Parameters</span></span>
 <span data-ttu-id="905a1-121">✔️ 매개 변수 형식으로 사용할 수 있는 최소 특수화 된 형식을 사용 합니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-121">✔️ DO use the least-specialized type possible as a parameter type.</span></span> <span data-ttu-id="905a1-122">컬렉션을 매개 변수로 사용 하는 대부분의 멤버는 `IEnumerable<T>` 인터페이스를 사용 합니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-122">Most members taking collections as parameters use the `IEnumerable<T>` interface.</span></span>

 <span data-ttu-id="905a1-123">❌ <xref:System.Collections.Generic.ICollection%601> 또는 <xref:System.Collections.ICollection>를 매개 변수로 사용 하 여 `Count` 속성에 액세스 하지 않도록 합니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-123">❌ AVOID using <xref:System.Collections.Generic.ICollection%601> or <xref:System.Collections.ICollection> as a parameter just to access the `Count` property.</span></span>

 <span data-ttu-id="905a1-124">대신 `IEnumerable<T>` 또는 `IEnumerable`를 사용 하 고 개체가 `ICollection<T>` 또는 `ICollection`를 구현 하는지 여부를 동적으로 확인 하는 것이 좋습니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-124">Instead, consider using `IEnumerable<T>` or `IEnumerable` and dynamically checking whether the object implements `ICollection<T>` or `ICollection`.</span></span>

## <a name="collection-properties-and-return-values"></a><span data-ttu-id="905a1-125">컬렉션 속성 및 반환 값</span><span class="sxs-lookup"><span data-stu-id="905a1-125">Collection Properties and Return Values</span></span>
 <span data-ttu-id="905a1-126">❌ 설정할 수 있는 컬렉션 속성을 제공 하지 않습니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-126">❌ DO NOT provide settable collection properties.</span></span>

 <span data-ttu-id="905a1-127">사용자는 컬렉션을 먼저 지운 다음 새 내용을 추가 하 여 컬렉션의 콘텐츠를 바꿀 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-127">Users can replace the contents of the collection by clearing the collection first and then adding the new contents.</span></span> <span data-ttu-id="905a1-128">전체 컬렉션을 바꾸는 것이 일반적인 시나리오 인 경우 컬렉션에 `AddRange` 메서드를 제공 하는 것이 좋습니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-128">If replacing the whole collection is a common scenario, consider providing the `AddRange` method on the collection.</span></span>

 <span data-ttu-id="905a1-129">속성 또는 읽기/쓰기 컬렉션을 나타내는 반환 값에 대해 `Collection<T>`의 서브 클래스 또는 `Collection<T>`를 사용 ✔️ 합니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-129">✔️ DO use `Collection<T>` or a subclass of `Collection<T>` for properties or return values representing read/write collections.</span></span>

 <span data-ttu-id="905a1-130">`Collection<T>` 일부 요구 사항 (예: 컬렉션에서 <xref:System.Collections.IList>를 구현 하지 않아야 함)을 충족 하지 않는 경우 `IEnumerable<T>`, `ICollection<T>`또는 <xref:System.Collections.Generic.IList%601>을 구현 하 여 사용자 지정 컬렉션을 사용 합니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-130">If `Collection<T>` does not meet some requirement (e.g., the collection must not implement <xref:System.Collections.IList>), use a custom collection by implementing `IEnumerable<T>`, `ICollection<T>`, or <xref:System.Collections.Generic.IList%601>.</span></span>

 <span data-ttu-id="905a1-131"><xref:System.Collections.ObjectModel.ReadOnlyCollection%601>, `ReadOnlyCollection<T>`의 서브 클래스를 사용 하거나, 드문 경우 이지만 속성 또는 읽기 전용 컬렉션을 나타내는 값을 반환 하는 `IEnumerable<T>` 경우를 ✔️ 합니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-131">✔️ DO use <xref:System.Collections.ObjectModel.ReadOnlyCollection%601>, a subclass of `ReadOnlyCollection<T>`, or in rare cases `IEnumerable<T>` for properties or return values representing read-only collections.</span></span>

 <span data-ttu-id="905a1-132">일반적으로 `ReadOnlyCollection<T>`를 사용 하는 것이 좋습니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-132">In general, prefer `ReadOnlyCollection<T>`.</span></span> <span data-ttu-id="905a1-133">일부 요구 사항 (예: 컬렉션에서 `IList`을 구현 하지 않아야 함)을 충족 하지 않는 경우 `IEnumerable<T>`, `ICollection<T>`또는 `IList<T>`를 구현 하 여 사용자 지정 컬렉션을 사용 합니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-133">If it does not meet some requirement (e.g., the collection must not implement `IList`), use a custom collection by implementing `IEnumerable<T>`, `ICollection<T>`, or `IList<T>`.</span></span> <span data-ttu-id="905a1-134">사용자 지정 읽기 전용 컬렉션을 구현 하는 경우 `true`을 반환 하 `ICollection<T>.IsReadOnly`를 구현 합니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-134">If you do implement a custom read-only collection, implement `ICollection<T>.IsReadOnly` to return `true`.</span></span>

 <span data-ttu-id="905a1-135">지원 하려는 시나리오가 앞 으로만 반복 되는 경우에만 `IEnumerable<T>`를 사용 하면 됩니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-135">In cases where you are sure that the only scenario you will ever want to support is forward-only iteration, you can simply use `IEnumerable<T>`.</span></span>

 <span data-ttu-id="905a1-136">컬렉션을 직접 사용 하는 대신 제네릭 기본 컬렉션의 서브 클래스를 사용 하는 것이 좋습니다 ✔️.</span><span class="sxs-lookup"><span data-stu-id="905a1-136">✔️ CONSIDER using subclasses of generic base collections instead of using the collections directly.</span></span>

 <span data-ttu-id="905a1-137">이를 통해 더 나은 이름을 제공 하 고 기본 컬렉션 형식에 없는 도우미 멤버를 추가할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-137">This allows for a better name and for adding helper members that are not present on the base collection types.</span></span> <span data-ttu-id="905a1-138">이는 특히 높은 수준의 Api에 적용 됩니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-138">This is especially applicable to high-level APIs.</span></span>

 <span data-ttu-id="905a1-139">✔️ 일반적으로 사용 되는 메서드 및 속성에서 `Collection<T>` 또는 `ReadOnlyCollection<T>`의 서브 클래스를 반환 하는 것이 좋습니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-139">✔️ CONSIDER returning a subclass of `Collection<T>` or `ReadOnlyCollection<T>` from very commonly used methods and properties.</span></span>

 <span data-ttu-id="905a1-140">이를 통해 도우미 메서드를 추가 하거나 나중에 컬렉션 구현을 변경할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-140">This will make it possible for you to add helper methods or change the collection implementation in the future.</span></span>

 <span data-ttu-id="905a1-141">컬렉션에 저장 된 항목에 고유 키 (이름, Id 등)가 있는 경우 키가 지정 된 컬렉션을 사용 하는 것이 좋습니다. ✔️</span><span class="sxs-lookup"><span data-stu-id="905a1-141">✔️ CONSIDER using a keyed collection if the items stored in the collection have unique keys (names, IDs, etc.).</span></span> <span data-ttu-id="905a1-142">키가 지정 된 컬렉션은 정수 및 키 둘 다로 인덱싱할 수 있는 컬렉션이 며 일반적으로 `KeyedCollection<TKey,TItem>`에서 상속 하 여 구현 됩니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-142">Keyed collections are collections that can be indexed by both an integer and a key and are usually implemented by inheriting from `KeyedCollection<TKey,TItem>`.</span></span>

 <span data-ttu-id="905a1-143">키 컬렉션은 일반적으로 더 큰 메모리 차지을 가지 며, 메모리 오버 헤드가 키가 있는 이점 보다 큰 경우에는 사용 하지 않아야 합니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-143">Keyed collections usually have larger memory footprints and should not be used if the memory overhead outweighs the benefits of having the keys.</span></span>

 <span data-ttu-id="905a1-144">❌ 컬렉션 속성 또는 컬렉션을 반환 하는 메서드에서 null 값을 반환 하지 않습니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-144">❌ DO NOT return null values from collection properties or from methods returning collections.</span></span> <span data-ttu-id="905a1-145">대신 빈 컬렉션이 나 빈 배열을 반환 합니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-145">Return an empty collection or an empty array instead.</span></span>

 <span data-ttu-id="905a1-146">일반 규칙은 null 및 비어 있는 (0 개 항목) 컬렉션 또는 배열을 동일 하 게 처리 해야 한다는 것입니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-146">The general rule is that null and empty (0 item) collections or arrays should be treated the same.</span></span>

### <a name="snapshots-versus-live-collections"></a><span data-ttu-id="905a1-147">스냅숏과 라이브 컬렉션 비교</span><span class="sxs-lookup"><span data-stu-id="905a1-147">Snapshots Versus Live Collections</span></span>
 <span data-ttu-id="905a1-148">특정 시점의 상태를 나타내는 컬렉션을 스냅숏 컬렉션 이라고 합니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-148">Collections representing a state at some point in time are called snapshot collections.</span></span> <span data-ttu-id="905a1-149">예를 들어 데이터베이스 쿼리에서 반환 된 행을 포함 하는 컬렉션은 스냅숏이 됩니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-149">For example, a collection containing rows returned from a database query would be a snapshot.</span></span> <span data-ttu-id="905a1-150">항상 현재 상태를 나타내는 컬렉션을 라이브 컬렉션 이라고 합니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-150">Collections that always represent the current state are called live collections.</span></span> <span data-ttu-id="905a1-151">예를 들어 `ComboBox` 항목의 컬렉션은 라이브 컬렉션입니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-151">For example, a collection of `ComboBox` items is a live collection.</span></span>

 <span data-ttu-id="905a1-152">❌는 속성에서 스냅숏 컬렉션을 반환 하지 않습니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-152">❌ DO NOT return snapshot collections from properties.</span></span> <span data-ttu-id="905a1-153">속성은 라이브 컬렉션을 반환 해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-153">Properties should return live collections.</span></span>

 <span data-ttu-id="905a1-154">Getter 속성은 매우 간단한 작업 이어야 합니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-154">Property getters should be very lightweight operations.</span></span> <span data-ttu-id="905a1-155">스냅숏을 반환 하려면 O (n) 작업에서 내부 컬렉션의 복사본을 만들어야 합니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-155">Returning a snapshot requires creating a copy of an internal collection in an O(n) operation.</span></span>

 <span data-ttu-id="905a1-156">✔️는 스냅숏 컬렉션이 나 라이브 `IEnumerable<T>` (또는 해당 하위 형식)를 사용 하 여 휘발성 컬렉션을 나타냅니다. 즉, 컬렉션을 명시적으로 수정 하지 않고 변경할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-156">✔️ DO use either a snapshot collection or a live `IEnumerable<T>` (or its subtype) to represent collections that are volatile (i.e., that can change without explicitly modifying the collection).</span></span>

 <span data-ttu-id="905a1-157">일반적으로 공유 리소스를 나타내는 모든 컬렉션 (예: 디렉터리의 파일)은 일시적입니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-157">In general, all collections representing a shared resource (e.g., files in a directory) are volatile.</span></span> <span data-ttu-id="905a1-158">구현이 단순히 전방 전용 열거자 인 경우를 제외 하 고 이러한 컬렉션을 라이브 컬렉션으로 구현 하는 것은 매우 어렵거나 불가능 합니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-158">Such collections are very difficult or impossible to implement as live collections unless the implementation is simply a forward-only enumerator.</span></span>

## <a name="choosing-between-arrays-and-collections"></a><span data-ttu-id="905a1-159">배열 및 컬렉션 중에서 선택</span><span class="sxs-lookup"><span data-stu-id="905a1-159">Choosing Between Arrays and Collections</span></span>
 <span data-ttu-id="905a1-160">✔️ 배열에 대 한 컬렉션을 선호 합니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-160">✔️ DO prefer collections over arrays.</span></span>

 <span data-ttu-id="905a1-161">컬렉션은 내용에 대 한 제어를 강화 하 고, 시간이 지남에 따라 진화 하 고, 더 사용할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-161">Collections provide more control over contents, can evolve over time, and are more usable.</span></span> <span data-ttu-id="905a1-162">또한 읽기 전용 시나리오에 배열을 사용 하지 않는 것이 좋습니다. 배열을 복제 하는 비용이 너무 높을 수 있기 때문입니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-162">In addition, using arrays for read-only scenarios is discouraged because the cost of cloning the array is prohibitive.</span></span> <span data-ttu-id="905a1-163">유용성 연구에서는 일부 개발자가 컬렉션 기반 Api를 사용 하는 것이 더 편안 하다는 것을 보여 줍니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-163">Usability studies have shown that some developers feel more comfortable using collection-based APIs.</span></span>

 <span data-ttu-id="905a1-164">그러나 낮은 수준의 Api를 개발 하는 경우 읽기/쓰기 시나리오에 배열을 사용 하는 것이 더 좋을 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-164">However, if you are developing low-level APIs, it might be better to use arrays for read-write scenarios.</span></span> <span data-ttu-id="905a1-165">배열의 메모리 공간이 작을수록 작업 집합을 줄이고 배열의 요소에 대 한 액세스는 런타임에 의해 최적화 되기 때문에 더 빠릅니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-165">Arrays have a smaller memory footprint, which helps reduce the working set, and access to elements in an array is faster because it is optimized by the runtime.</span></span>

 <span data-ttu-id="905a1-166">메모리 사용을 최소화 하 고 성능을 최대화 하려면 하위 수준 Api의 배열을 사용 하는 것이 좋습니다. ✔️</span><span class="sxs-lookup"><span data-stu-id="905a1-166">✔️ CONSIDER using arrays in low-level APIs to minimize memory consumption and maximize performance.</span></span>

 <span data-ttu-id="905a1-167">✔️ 바이트 컬렉션 대신 바이트 배열을 사용 합니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-167">✔️ DO use byte arrays instead of collections of bytes.</span></span>

 <span data-ttu-id="905a1-168">속성이 getter를 호출할 때마다 속성이 새 배열 (예: 내부 배열의 복사본)을 반환 해야 하는 경우 속성에 배열을 사용 하지 ❌.</span><span class="sxs-lookup"><span data-stu-id="905a1-168">❌ DO NOT use arrays for properties if the property would have to return a new array (e.g., a copy of an internal array) every time the property getter is called.</span></span>

## <a name="implementing-custom-collections"></a><span data-ttu-id="905a1-169">사용자 지정 컬렉션 구현</span><span class="sxs-lookup"><span data-stu-id="905a1-169">Implementing Custom Collections</span></span>
 <span data-ttu-id="905a1-170">✔️ 새 컬렉션을 디자인할 때 `Collection<T>`, `ReadOnlyCollection<T>`또는 `KeyedCollection<TKey,TItem>`에서 상속 하는 것이 좋습니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-170">✔️ CONSIDER inheriting from `Collection<T>`, `ReadOnlyCollection<T>`, or `KeyedCollection<TKey,TItem>` when designing new collections.</span></span>

 <span data-ttu-id="905a1-171">새 컬렉션을 디자인할 때 `IEnumerable<T>` 구현 ✔️ 합니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-171">✔️ DO implement `IEnumerable<T>` when designing new collections.</span></span> <span data-ttu-id="905a1-172">`ICollection<T>` 또는 `IList<T>`를 구현 하는 것이 좋습니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-172">Consider implementing `ICollection<T>` or even `IList<T>` where it makes sense.</span></span>

 <span data-ttu-id="905a1-173">이러한 사용자 지정 컬렉션을 구현 하는 경우 `Collection<T>`에 의해 설정 된 API 패턴을 따르고 최대한 긴밀 하 게 `ReadOnlyCollection<T>` 합니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-173">When implementing such custom collection, follow the API pattern established by `Collection<T>` and `ReadOnlyCollection<T>` as closely as possible.</span></span> <span data-ttu-id="905a1-174">즉, 동일한 멤버를 명시적으로 구현 하 고, 이러한 두 컬렉션과 같은 매개 변수의 이름을로 지정할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-174">That is, implement the same members explicitly, name the parameters like these two collections name them, and so on.</span></span>

 <span data-ttu-id="905a1-175">이러한 인터페이스를 입력으로 사용할 때 컬렉션을 Api에 자주 전달 하는 경우 제네릭이 아닌 컬렉션 인터페이스 (`IList` 및 `ICollection`)를 구현 하는 것이 좋습니다. ✔️</span><span class="sxs-lookup"><span data-stu-id="905a1-175">✔️ CONSIDER implementing nongeneric collection interfaces (`IList` and `ICollection`) if the collection will often be passed to APIs taking these interfaces as input.</span></span>

 <span data-ttu-id="905a1-176">컬렉션의 개념과 관련이 없는 복잡 한 Api를 사용 하 여 형식에 컬렉션 인터페이스를 구현 하지 않도록 ❌ 합니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-176">❌ AVOID implementing collection interfaces on types with complex APIs unrelated to the concept of a collection.</span></span>

 <span data-ttu-id="905a1-177">❌ `CollectionBase`와 같은 제네릭이 아닌 기본 컬렉션에서 상속 되지 않습니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-177">❌ DO NOT inherit from nongeneric base collections such as `CollectionBase`.</span></span> <span data-ttu-id="905a1-178">대신 `Collection<T>`, `ReadOnlyCollection<T>` 및 `KeyedCollection<TKey,TItem>`를 사용해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-178">Use `Collection<T>`, `ReadOnlyCollection<T>`, and `KeyedCollection<TKey,TItem>` instead.</span></span>

### <a name="naming-custom-collections"></a><span data-ttu-id="905a1-179">사용자 지정 컬렉션 이름 지정</span><span class="sxs-lookup"><span data-stu-id="905a1-179">Naming Custom Collections</span></span>
 <span data-ttu-id="905a1-180">컬렉션 (`IEnumerable`을 구현 하는 형식)은 주로 (1) 구조 관련 작업을 포함 하는 새 데이터 구조를 만들고 기존 데이터 구조 (예: <xref:System.Collections.Generic.List%601>, <xref:System.Collections.Generic.LinkedList%601>, <xref:System.Collections.Generic.Stack%601>)와 다른 성능 특성을 만들고 (2) 특정 항목 집합을 포함 하는 특수 한 컬렉션을 만드는 (예: <xref:System.Collections.Specialized.StringCollection>) 두 가지 이유로 생성 됩니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-180">Collections (types that implement `IEnumerable`) are created mainly for two reasons: (1) to create a new data structure with structure-specific operations and often different performance characteristics than existing data structures (e.g.,  <xref:System.Collections.Generic.List%601>, <xref:System.Collections.Generic.LinkedList%601>, <xref:System.Collections.Generic.Stack%601>), and (2) to create a specialized collection for holding a specific set of items (e.g.,  <xref:System.Collections.Specialized.StringCollection>).</span></span> <span data-ttu-id="905a1-181">데이터 구조는 응용 프로그램 및 라이브러리의 내부 구현에서 주로 사용 됩니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-181">Data structures are most often used in the internal implementation of applications and libraries.</span></span> <span data-ttu-id="905a1-182">특수 컬렉션은 주로 Api (속성 및 매개 변수 형식)로 노출 됩니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-182">Specialized collections are mainly to be exposed in APIs (as property and parameter types).</span></span>

 <span data-ttu-id="905a1-183">✔️ `IDictionary` 또는 `IDictionary<TKey,TValue>`를 구현 하는 추상화 이름에 "Dictionary" 접미사를 사용 합니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-183">✔️ DO use the "Dictionary" suffix in names of abstractions implementing `IDictionary` or `IDictionary<TKey,TValue>`.</span></span>

 <span data-ttu-id="905a1-184">✔️ `IEnumerable` (또는 해당 하위 항목)를 구현 하 고 항목 목록을 나타내는 형식의 이름에 "Collection" 접미사를 사용 합니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-184">✔️ DO use the "Collection" suffix in names of types implementing `IEnumerable` (or any of its descendants) and representing a list of items.</span></span>

 <span data-ttu-id="905a1-185">사용자 지정 데이터 구조에 적합 한 데이터 구조 이름을 사용 ✔️ 합니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-185">✔️ DO use the appropriate data structure name for custom data structures.</span></span>

 <span data-ttu-id="905a1-186">❌ 컬렉션 추상화 이름에 "System.collections.generic.linkedlist" 또는 "Hashtable"와 같은 접미사의 특정 구현을 사용 하지 않는 것이 좋습니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-186">❌ AVOID using any suffixes implying particular implementation, such as "LinkedList" or "Hashtable," in names of collection abstractions.</span></span>

 <span data-ttu-id="905a1-187">컬렉션 이름에 항목 형식의 이름을 접두사로 사용 하는 것이 좋습니다 ✔️.</span><span class="sxs-lookup"><span data-stu-id="905a1-187">✔️ CONSIDER prefixing collection names with the name of the item type.</span></span> <span data-ttu-id="905a1-188">예를 들어 `Address` 형식의 항목을 저장 하는 컬렉션 (`IEnumerable<Address>`구현)은 `AddressCollection`이름 이어야 합니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-188">For example, a collection storing items of type `Address` (implementing `IEnumerable<Address>`) should be named `AddressCollection`.</span></span> <span data-ttu-id="905a1-189">항목 형식이 인터페이스 이면 항목 형식의 "I" 접두사를 생략할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-189">If the item type is an interface, the "I" prefix of the item type can be omitted.</span></span> <span data-ttu-id="905a1-190">따라서 <xref:System.IDisposable> 항목의 컬렉션을 `DisposableCollection`호출할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-190">Thus, a collection of <xref:System.IDisposable> items can be called `DisposableCollection`.</span></span>

 <span data-ttu-id="905a1-191">해당 하는 쓰기 가능한 컬렉션이 추가 될 수도 있고 프레임 워크에 이미 있는 경우 읽기 전용 컬렉션의 이름에 "ReadOnly" 접두사를 사용 하는 것이 좋습니다. ✔️</span><span class="sxs-lookup"><span data-stu-id="905a1-191">✔️ CONSIDER using the "ReadOnly" prefix in names of read-only collections if a corresponding writeable collection might be added or already exists in the framework.</span></span>

 <span data-ttu-id="905a1-192">예를 들어 `ReadOnlyStringCollection`읽기 전용 문자열 컬렉션을 호출 해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="905a1-192">For example, a read-only collection of strings should be called `ReadOnlyStringCollection`.</span></span>

 <span data-ttu-id="905a1-193">*2005, 2009 Microsoft Corporation © 부분입니다. All rights reserved.*</span><span class="sxs-lookup"><span data-stu-id="905a1-193">*Portions © 2005, 2009 Microsoft Corporation. All rights reserved.*</span></span>

 <span data-ttu-id="905a1-194">*Pearson Education, Inc의 동의로 재인쇄. 출처: [Framework Design Guidelines: Conventions, Idioms, and Patterns for Reusable .NET Libraries, 2nd Edition](https://www.informit.com/store/framework-design-guidelines-conventions-idioms-and-9780321545619) 작성자: Krzysztof Cwalina 및 Brad Abrams, 출판 정보: Oct 22, 2008 by Addison-Wesley Professional as part of the Microsoft Windows Development Series.*</span><span class="sxs-lookup"><span data-stu-id="905a1-194">*Reprinted by permission of Pearson Education, Inc. from [Framework Design Guidelines: Conventions, Idioms, and Patterns for Reusable .NET Libraries, 2nd Edition](https://www.informit.com/store/framework-design-guidelines-conventions-idioms-and-9780321545619) by Krzysztof Cwalina and Brad Abrams, published Oct 22, 2008 by Addison-Wesley Professional as part of the Microsoft Windows Development Series.*</span></span>

## <a name="see-also"></a><span data-ttu-id="905a1-195">참고 항목</span><span class="sxs-lookup"><span data-stu-id="905a1-195">See also</span></span>

- [<span data-ttu-id="905a1-196">프레임워크 디자인 지침</span><span class="sxs-lookup"><span data-stu-id="905a1-196">Framework Design Guidelines</span></span>](../../../docs/standard/design-guidelines/index.md)
- [<span data-ttu-id="905a1-197">사용 지침</span><span class="sxs-lookup"><span data-stu-id="905a1-197">Usage Guidelines</span></span>](../../../docs/standard/design-guidelines/usage-guidelines.md)
